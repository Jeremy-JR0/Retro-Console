#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <time.h>



int main(int argc, char *argv[]) {
    if (argc < 2) {
        exit(EXIT_STATUS)
    }

    int length = strlen(argv[]);
    int temp = argv[i];

    for(int i = 1; i <= length; i++) {
        
        if ((temp%1073741824)%10 == 0) {
            printf("%i", temp/)
        }
    }
}
